## Dependencies

[Pillow 12.1.0](https://pypi.org/project/pillow/)
pip install pillow

## TODO
Rewrite Color to be a float (linear) based class, write reusable methods, derive from tuple[]/iter to be able to use with handy methods. Only convert at perimeter